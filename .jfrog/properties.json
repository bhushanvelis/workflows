{
  "build.name": "service1",                     // repo name
  "build.number": "${{ github.run_id }}",        // GITHUB_RUN_ID
  "git.branch": "${{ github.ref_name }}",        // Branch name
  "git.commit": "${{ github.sha }}",             // Commit SHA
  "version": "1.0.0",                            // Static or bump via workflow
  "ci.url": "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}",
  "ci.workflow": "${{ github.workflow }}"        // Workflow name
}
